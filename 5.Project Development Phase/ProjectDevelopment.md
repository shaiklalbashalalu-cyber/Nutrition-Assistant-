# Phase 5: Project Development 💻

This is the execution phase. The actual source code for this project is located in the root directory under the `client` and `server` folders.

## Codebase Navigation
* **[`/client`](../client):** Contains all frontend React/Next.js code.
* **[`/server`](../server):** Contains the backend Node.js API and database logic.

## Development Standards
* Git branching strategy (e.g., `main`, `dev`, `feature/name`).
* Naming conventions and folder structuring inside the codebase.
* Instructions on how to run both servers concurrently (e.g., using `npm run dev`).
